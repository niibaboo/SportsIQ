name: Player Stat Model

on:
  schedule:
    - cron: '0 12 * * *'  # 12:00 PM UTC daily
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install requests
      - name: Generate Player Stat Model
        env:
          THESTATSAPI_KEY: ${{ secrets.THESTATSAPI_KEY }}
        run: python3 player_stat_model.py --auto
      - name: Copy data to docs
        run: |
          mkdir -p docs/player-stat-model
          if [ -f player_stats_data.json ]; then
            cp player_stats_data.json docs/player-stat-model/player_stats_data.json
            echo "✓ Copied player_stats_data.json"
          fi
          if [ -f player_stat_model.html ]; then
            cp player_stat_model.html docs/player-stat-model/index.html
            echo "✓ Copied index.html"
          fi
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/player-stat-model/
          git commit -m "Player Stat Model update $(date -u +'%Y-%m-%d %H:%M UTC')" || true
          git push
